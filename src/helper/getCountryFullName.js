var getCountryFullName = function(country) {
    switch (country) {
        case "tw":
            return "Taiwan";
            break;
        case "us":
            return "USA";
            break;
    }
};

export default getCountryFullName;
